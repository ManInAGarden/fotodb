using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using HSp.CsEpo;

namespace HSp.FotoDB
{
    public class ViewKamera : EpoView
    {
        public ViewKamera()
            : base()
        {
        }

         public ViewKamera(Epo viewedEpo, object parent)
            : base(viewedEpo, parent)
        { }

        protected override void InitSpecialControls()
        {
            base.InitSpecialControls();

            SetLabel("Name", "Name");
            SetGeometry("Name", 0, 0, 14, 0, 40, 1);

        }
    }
}
